# Regenerated corpus — 476 MB, rebuilt from the pinned APIs.guru commit.
# Not committed by design (see README.md sections 1-2).
specs/clean/

# Python bytecode cache
__pycache__/
*.pyc

# Uniform OpenAPI 3.x + JSON corpus, regenerated by src/convert.py.
specs/clean3/

# Fetched corpora, pinned in config.py and rebuilt per README.md section 2.
openapi-directory/
rama-thresholds/

# swagger2openapi, installed with: npm install swagger2openapi@7.0.8
node_modules/
package-lock.json

# Checkpointed scan phase of src/filter.py (regenerable).
.cache/

# Parsed per-service documents (regenerated by src/parse_spec.py).
parsed/

# The proposal document. Deliberately NOT committed: this repository is
# public, and the proposal is unpublished research (hypotheses, experimental
# design, three-week plan). Remove this line if you decide otherwise.
ILSC_Proposal_v3.docx
